# Changesets

Add one changeset for each user-facing package change. Run `bunx changeset` to create one, select the affected workspace packages, and choose the semver bump. The release workflow opens or updates a version PR; merging it publishes the packages to npm.
